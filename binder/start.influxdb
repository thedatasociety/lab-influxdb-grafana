#!/bin/bash

mkdir -p ~/.config/influxctl
cp  ~/resources/configs/influxdb-cli/* ~/.config/influxctl/

echo "Starting influxd"

nohup influxdb3 serve --without-auth  --node-id localhost-node &> ~/logs/influxdb-$INFLUXDB_VERSION.log &

sleep 5